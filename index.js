//app namespace - common function
var app = app || {};
 
// app page specific namespace
app.pg = app.pg || {};

// app common function
(function(o){
    o.init = function(){
    }

}(app));

// initialize  app

$(function(){

    app.init();

});